Marketing website (Wordpress?)
----------------------------
Free - otherwise paid-for if we host and support
Chatbot / CRM integration
Landing pages / Google Ads / Facebook Ads
API documentation - swagger


Ultra Lightweight MVP
----------------------------------
1) Client Setup
    --get, id is optional so to return a list
    GET /client/?{id}

    define pagination?
        &limit=10,20,50,100
        &page=1,2,3,4...

    --create
    POST /client
    +json body

    --update
    PUT /client/{id}
    +json body

    --remove
    DELETE /client/{id}


6) Campaign Setup
    --get, id is optional so to return a list
    GET /campaign/?{id}

    define pagination?
        &limit=10,20,50,100
        &page=1,2,3,4...

    --create
    POST /campaign
    +json body

    --update
    PUT /campaign/{id}
    +json body

    --remove
    DELETE /campaign/{id}



    --START SIMPLE
        --build out according to examples of more complex setups

    details managed by json col in campaign
    Message Schedules
        - set this in the campaign

    Unsubscribe page
        - need global unsubscribe verbage
        - support custom client message
        - support customer override

    Confirmation page
        - need global confirmation verbage
        - support custom unsubscribe message
        - support customer override

    Rule sets for delivery
        - this is set in the campaign settings
        - per rules set the contact date
        - support high-level logic? contact method is x if data.phone is blank

    Require message review before queueing data?
        - feature not supported in MVP

    Message Types
        -Appointment Reminder
        -Announcement
        -Home For sale
        -Home Shortsale
        -Home Auction
        -No Show
        -Reschedule
        -Lab
        -Immunization
        etc

2) Data upload / packet progress
    --get, id is optional so to return a list
    GET /packet/?{id}

    define pagination?
        &limit=10,20,50,100
        &page=1,2,3,4...

    --create
    POST /packet
    +json body

    --update
    PUT /packet/{id}
    +json body

    --remove
    DELETE /packet/{id}


3) Billing
    -buy credits
    -see transaction history

    GET /purchase
        --purchase credits
    POST /purchase
        --purchase via Swipe?

    GET /credits


4) Client Dashboard
    -see balance of credits
    -see numbers of notifications delivered (24hr rotating, 3/7/30 days)
    -end user response %
    -alert/alarm for x% credits left

    GET /dashboard

5) Report
    -contacts per data packet

    GET /report/delivered
    GET /report/response
Security
    Users

        --get, id is optional so to return a list
        GET /user/?{id}

        define pagination?
            &limit=10,20,50,100
            &page=1,2,3,4...

        --create
        POST /user
        +json body

        --update
        PUT /user/{id}
        +json body

        --remove
        DELETE /user/{id}


    lost password

    GET /user/forgotpassword
    POST /user/forgotpassword

    Policies

        --get, id is optional so to return a list
        GET /policy/?{id}

        define pagination?
            &limit=10,20,50,100
            &page=1,2,3,4...

        --create
        POST /policy
        +json body

        --update
        PUT /policy/{id}
        +json body

        --remove
        DELETE /policy/{id}

    Roles

        --get, id is optional so to return a list
        GET /role/?{id}

        define pagination?
            &limit=10,20,50,100
            &page=1,2,3,4...

        --create
        POST /role
        +json body

        --update
        PUT /role/{id}
        +json body

        --remove
        DELETE /role/{id}



Back-end Platform MVP Features
----------------------------------
Client Setup

Campaigns
    details managed by json col in campaign
    Message Schedules
        - set this in the campaign

    Unsubscribe page
        - need global unsubscribe verbage
        - support custom client message
        - support customer override

    Confirmation page
        - need global confirmation verbage
        - support custom unsubscribe message
        - support customer override

    Rule sets for delivery
        - this is set in the campaign settings
        - per rules set the contact date
        - support high-level logic? contact method is x if data.phone is blank

    Require message review before queueing data?
        - feature not supported in MVP

    Message Types
        -Appointment Reminder
        -Announcement
        -Home For sale
        -Home Shortsale
        -Home Auction
        -No Show
        -Reschedule
        -Lab
        -Immunization
        etc

Missed Contacts
    -need UI for this -- support ONLY

Time for data packet to get into system
    -steps are logged in tables

Throttle Settings
    - Phone Calls only?
    - Not slated for MVP

Customer Block
    -also show unsubscribed
        -can work some kind of follow-up for patient

-Incoming Data
    - data packet tracking

-Data Loading
    - this is supported by data_packet and data_packet_rows
    -ENTERPRISE: support better transaction assuredness -- client reports an md5sum of the data packet as do we, else the transaction reports an incomplete packet transmission

-Field Mapping
    - load data into initial area via load maps table
    - supported by load map table

    -Cleaning / Field Manipulation
        - perform operations against the data
        - this is supported by sprocs and field mapping

    - Message Assembly
        - this is supported by sprocs and field mapping

Message Review
    - allow client to view messages as they will be delivered
    - sms, email and phone message

-End-user subscription settings
    -manage subscription settings

-Billing - billing table
    --billing is a purchase process
    --purchase history

-Reporting - supported by message history


Support
    FAQ
    User Guide / Knowledgebase
    Online chat
    Contact Us
    --hook into ticketing system (CRM?)

Security
    Users
        lost password
        password expiration
        2FA -- supported by phone number and email address in user table
            other portion of the process is exterior to the db
        backup email

    Policies
    Roles
    Groups